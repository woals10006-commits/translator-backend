# API key is read from the CLAUDE_API_KEY environment variable — never commit the key.
claude.api.key=${CLAUDE_API_KEY:}
spring.servlet.multipart.max-file-size=50MB
spring.servlet.multipart.max-request-size=50MB
server.port=8080
